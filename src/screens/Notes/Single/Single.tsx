import React from 'react'
import { Text, View } from 'react-native'

export const Single = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text>Notes Single</Text>
    </View>
  )
}
