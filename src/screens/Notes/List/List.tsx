import React from 'react'
import { Text, View } from 'react-native'

export const List = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text>Notes List</Text>
    </View>
  )
}
