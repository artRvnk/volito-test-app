import React from 'react'
import { Text, View } from 'react-native'

export const Edit = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text>Notes Edit</Text>
    </View>
  )
}
