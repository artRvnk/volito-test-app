import React from 'react'
import { Text, View } from 'react-native'

export const Main = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text>Auth Main</Text>
    </View>
  )
}
